[package]
name = "kd-core"
description = "Deterministic simulation core and binary protocol for KD Hybrid"
version.workspace = true
edition.workspace = true
license = "MIT AND MPL-2.0"
repository.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["rlib"]

[lints]
workspace = true
